server:
  port: 8081

spring:
  application:
    name: QUIZ-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/microservices_db
    username: root
    password: Pooja@123#
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

eureka:
  instance:
    client:
      server-url:
        default-zone: http://localhost:8761/eureka/